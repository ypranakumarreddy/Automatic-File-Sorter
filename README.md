# Automatic-File-Sorter
Python script that automatically sorts files in a folder into subfolders by file type (CSV, images, text, documents, PDFs). Demonstrates file handling and automation using Python.
